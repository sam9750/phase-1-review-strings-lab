const currentUser = `Sam`;
const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`; 
const excitedWelcomeMessage = welcomeMessage.toUpperCase();
const shortGreeting1 = `Welcome, ${currentUser}`;
const shortGreeting = `Welcome, ${currentUser.slice(0, 1)}!`; //gets first letter of current user's name, starts at index 0 and goes to index 1


